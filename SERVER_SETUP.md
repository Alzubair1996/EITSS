# 🖥️ دليل تشغيل النظام كسيرفر على الشبكة المحلية

## الفكرة العامة

النظام (قاريب) هو تطبيق ويب — يعني يعمل تلقائياً كسيرفر.
الجهاز الرئيسي يشغّل التطبيق، وبقية الأجهزة تتصل به عبر **المتصفح فقط** بدون تثبيت أي شيء.

```
┌─────────────────────────────┐
│      الجهاز الرئيسي (Server) │
│  ┌──────────┐ ┌───────────┐ │
│  │  قاريب   │ │  قاعدة   │ │
│  │  :9002   │ │ البيانات │ │
│  └──────────┘ └───────────┘ │
└──────────┬──────────────────┘
           │  شبكة واحدة (LAN / WiFi)
    ┌──────┴──────┐
    │             │
  جهاز 2       جهاز 3
  (متصفح)     (متصفح)
```

---

## 🚀 خطوات التشغيل

### الخطوة 1 — تشغيل السيرفر (الجهاز الرئيسي)

افتح ملف `start-server.bat` بالنقر المزدوج عليه.

أو شغّل الأمر التالي في مجلد المشروع:

```cmd
node server.js
```

سيظهر:
```
▲ Next.js
- Local:   http://localhost:9002
- Network: http://192.168.1.x:9002
```

---

### الخطوة 2 — معرفة IP الجهاز الرئيسي

افتح `cmd` واكتب:
```cmd
ipconfig
```

ابحث عن **IPv4 Address** مثل:
```
IPv4 Address. . . . . . . . . . . : 192.168.1.5
```

---

### الخطوة 3 — فتح المنفذ في جدار الحماية (مرة واحدة فقط)

شغّل `cmd` **كمسؤول (Run as Administrator)** ثم:

```cmd
netsh advfirewall firewall add rule name="QaareebApp" dir=in action=allow protocol=TCP localport=9002
```

---

### الخطوة 4 — الاتصال من بقية الأجهزة

افتح المتصفح في أي جهاز آخر على نفس الشبكة واكتب:
```
http://192.168.1.5:9002
```
غيّر `192.168.1.5` بـ IP الجهاز الرئيسي الفعلي.

---

## 🔄 تشغيل تلقائي مع بدء Windows

لإبقاء السيرفر يعمل دائماً حتى بعد إعادة التشغيل، استخدم **pm2**:

```cmd
npm install -g pm2

REM داخل مجلد standalone
pm2 start node --name qaareeb -- server.js

REM تفعيل التشغيل التلقائي
pm2 startup
pm2 save
```

---

## ⚙️ معلومات تقنية

| البند | التفصيل |
|---|---|
| **المنفذ الافتراضي** | `9002` |
| **قاعدة البيانات** | SQLite — ملف واحد على الجهاز الرئيسي |
| **بقية الأجهزة** | تحتاج فقط متصفح ويب |
| **الشبكة المطلوبة** | LAN أو WiFi — نفس الراوتر |
| **التزامن** | يدعم عدة مستخدمين في نفس الوقت |

---

## ❗ ملاحظات مهمة

- **لا تغلق** نافذة السيرفر على الجهاز الرئيسي أثناء العمل.
- جميع البيانات محفوظة في ملف `balla.sqlite` — احتفظ بنسخة احتياطية منه بانتظام.
- إذا غيّر الجهاز الرئيسي IP، عدّل الرابط في بقية الأجهزة.
- للحصول على IP ثابت، اضبط الجهاز الرئيسي بـ **Static IP** من إعدادات الشبكة.

---

*نظام قاريب — دليل تشغيل الشبكة*
