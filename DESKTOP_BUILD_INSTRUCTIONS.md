
# تعليمات بناء تطبيق سطح المكتب (Windows)

لقد تم إعداد المشروع بالكامل ليتم تحويله إلى تطبيق سطح مكتب أصلي لنظام ويندوز باستخدام إطار عمل Tauri. لإنشاء ملف التثبيت النهائي (`.msi`)، يرجى اتباع الخطوات التالية بدقة على جهاز ويندوز الخاص بك.

## الخطوة 1: تثبيت المتطلبات الأساسية

قبل بناء التطبيق، يجب عليك تثبيت الأدوات التالية. **هذه الخطوة تتم مرة واحدة فقط.**

### 1. Microsoft C++ Build Tools

أداة Tauri تحتاج إلى أدوات بناء ++C من مايكروسوفت.

1.  قم بتنزيل **Visual Studio 2022 Build Tools** من [هذا الرابط](https://visualstudio.microsoft.com/visual-cpp-build-tools/).
2.  قم بتشغيل المثبت.
3.  في قسم "Workloads"، اختر **"Desktop development with C++"** (تطوير سطح المكتب باستخدام ++C).
4.  في نافذة تفاصيل التثبيت على اليمين، تأكد من تحديد الخيارات التالية على الأقل:
    *   MSVC v143 - VS 2022 C++ x64/x86 build tools
    *   Windows 11 SDK (أو Windows 10 SDK إذا كنت تستخدم ويندوز 10)
5.  انقر على "Install" وانتظر حتى تكتمل العملية.

### 2. WebView2

هذا هو المتصفح المدمج الذي يستخدمه Tauri لعرض واجهة التطبيق. هو مثبت بشكل افتراضي على أنظمة Windows 10 و 11 الحديثة. إذا لم يكن مثبتًا لسبب ما، يمكنك تنزيله من [صفحة مايكروسوفت الرسمية](https://developer.microsoft.com/en-us/microsoft-edge/webview2/).

### 3. Rust

Tauri يستخدم لغة Rust لبناء الجزء الخلفي من التطبيق.

1.  اذهب إلى [rust-lang.org](https://www.rust-lang.org/tools/install).
2.  قم بتنزيل `rustup-init.exe` وتشغيله.
3.  سيفتح لك نافذة طرفية. اضغط `1` ثم `Enter` لاختيار الخيار الافتراضي ("Proceed with installation").
4.  بعد اكتمال التثبيت، قم بإعادة تشغيل الطرفية (Terminal/CMD/PowerShell) للتأكد من أن `cargo` (مدير حزم Rust) يعمل.

## الخطوة 2: بناء التطبيق

بعد تثبيت جميع المتطلبات بنجاح، أنت الآن جاهز لبناء النسخة النهائية من تطبيقك.

1.  افتح الطرفية (Command Prompt, PowerShell, أو Windows Terminal) في مجلد المشروع الرئيسي.
2.  قم بتشغيل الأمر التالي:

    ```bash
    npm run tauri:build
    ```

3.  ستبدأ عملية البناء. قد تستغرق هذه العملية بعض الوقت (خصوصًا في المرة الأولى) لأنها تقوم ببناء واجهة Next.js، ثم تقوم بترجمة كود Rust، وأخيرًا تقوم بتجميع كل شيء في ملف تثبيت واحد.

## الخطوة 3: العثور على ملف التثبيت

بعد انتهاء عملية البناء بنجاح، ستجد ملف التثبيت النهائي في المسار التالي داخل مجلد مشروعك:

`src-tauri/target/release/bundle/msi/`

سيكون اسم الملف شيئًا مثل `QareebStore_0.1.0_x64_en-US.msi`.

**هذا هو الملف الذي يمكنك توزيعه للمستخدمين.** عند تشغيله، سيقوم بتثبيت "Qareeb Store" على جهاز الكمبيوتر الخاص بهم كأي برنامج ويندوز آخر، مع أيقونة على سطح المكتب وفي قائمة "ابدأ".
